[Unit]
# wlanpi-config-at-startup writes /etc/wlanpi-model. Run after it so the
# ExecStartPost can read that cache instead of probing USB/PCIe at boot.
After=wlanpi-config-at-startup.service

[Service]
ExecStartPost=-/usr/bin/wlanpi-avahi-service-update