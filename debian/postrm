#!/bin/sh
set -e

case "$1" in
    remove|purge)
        # Stop advertising; the service file is generated, not shipped
        rm -f /etc/avahi/services/wlanpi_announce.service
        ;;
esac

#DEBHELPER#